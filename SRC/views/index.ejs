<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EMU Dynamic Exam Calendar</title>
    <link href="bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="custom.css" />
    <% if (umamiScriptUrl && umamiWebsiteId) { %>
    <script defer src="<%= umamiScriptUrl %>" data-website-id="<%= umamiWebsiteId %>"></script>
    <% } %>
  </head>

  <body>
    <div class="container py-5">
      <!-- Logo Section -->
      <div class="logo-box text-center mb-4">
        <img
          src="emu-logo.svg"
          alt="EMU Logo"
          class="img-fluid logo-image"
        />
      </div>

      <!-- Title Section -->
      <div class="text-center mb-4">
        <h1 class="display-5 fw-bold mb-2">
          <i class="bi bi-calendar-check me-2"></i>
          EMU Dynamic Exam Calendar
        </h1>
        <p class="text-muted">Organize your exam schedule with ease</p>
      </div>

      <!-- Disclaimer Alert -->
      <div class="alert alert-warning alert-modern text-center mb-3">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        <span class="fw-bold">Disclaimer:</span>
        This website is not partnered with or endorsed by Eastern Mediterranean
        University.
        <strong>This is a personal project and is not affiliated with the university.</strong>
      </div>

      <!-- Info Alert -->
      <div class="alert alert-info alert-modern text-center mb-4">
        <i class="bi bi-info-circle-fill me-2"></i>
        <span class="fw-bold">Info:</span>
        This website gets the exam dates from the official
        <a
          class="fw-semibold text-decoration-underline"
          href="https://exams.emu.edu.tr/midfinal/examlist.asp"
          target="_blank"
          >EMU exam schedule</a
        >
        and generates a dynamic calendar for the selected courses.
      </div>

      <!-- Input Section -->
      <div class="card card-modern mb-4">
        <div class="card-body">
          <label for="courseCode" class="form-label fw-semibold">
            <i class="bi bi-search me-2"></i>Enter course code:
          </label>
          <div class="input-group input-group-modern">
            <span class="input-group-text">
              <i class="bi bi-mortarboard-fill"></i>
            </span>
            <input
              type="text"
              id="courseCode"
              class="form-control"
              placeholder="e.g., ITEC310, ITEC420, ..."
            />
            <button class="btn btn-primary" onclick="addCourse()">
              <i class="bi bi-plus-circle me-1"></i>
              Add Course
            </button>
          </div>
          <small class="text-muted mt-2 d-block">
            <i class="bi bi-lightbulb me-1"></i>
            Tip: You can add multiple courses separated by commas
          </small>
        </div>
      </div>

      <!-- Table Section -->
      <div class="card card-modern mb-4">
        <div class="card-header bg-gradient">
          <h5 class="mb-0">
            <i class="bi bi-list-ul me-2"></i>
            Your Exam Schedule
          </h5>
        </div>
        <div class="card-body p-0">
          <div class="table-responsive">
            <table
              id="ExamTable"
              class="table table-modern table-hover text-center align-middle mb-0"
            >
              <thead>
                <tr>
                  <th><i class="bi bi-code-square me-1"></i>Course Code</th>
                  <th><i class="bi bi-book me-1"></i>Course Name</th>
                  <th><i class="bi bi-calendar-event me-1"></i>Exam Date</th>
                  <th><i class="bi bi-clock me-1"></i>Exam Time</th>
                  <th><i class="bi bi-gear me-1"></i>Actions</th>
                </tr>
              </thead>
              <tbody></tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Button Section -->
      <div class="row g-3 mb-4">
        <div class="col-md-4">
          <a
            id="generatedURL"
            href="#"
            class="btn btn-success btn-modern w-100 d-flex align-items-center justify-content-center"
            target="_blank"
          >
            <i class="bi bi-download me-2"></i>
            Download Calendar
          </a>
        </div>
        <div class="col-md-4">
          <a
            id="subscribeURL"
            href="#"
            class="btn btn-primary btn-modern w-100 d-flex align-items-center justify-content-center"
            target="_blank"
          >
            <i class="bi bi-apple me-2"></i>
            Subscribe Calendar
          </a>
        </div>
        <div class="col-md-4">
          <button
            class="btn btn-danger btn-modern w-100 d-flex align-items-center justify-content-center"
            onclick="clearAll();"
          >
            <i class="bi bi-trash3 me-2"></i>
            Clear All
          </button>
        </div>
      </div>

      <!-- Footer -->
      <div class="text-center text-muted mt-4">
        <small>
          <i class="bi bi-heart-fill text-danger"></i>
          Made with passion for EMU students
        </small>
      </div>
    </div>

    <script src="main.js"></script>

    <script
      data-name="BMC-Widget"
      data-cfasync="false"
      src="https://cdnjs.buymeacoffee.com/1.0.0/widget.prod.min.js"
      data-id="alii"
      data-description="Support me on Buy me a coffee!"
      data-message=""
      data-color="#024187"
      data-position="Right"
      data-x_margin="18"
      data-y_margin="18"
    ></script>
  </body>
</html>
